import React from 'react';

export default class ListR extends React.Component {
  static propTypes = {
    url: React.PropTypes.string,
  };

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div></div>
    );
  }
}
